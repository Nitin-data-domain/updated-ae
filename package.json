{
  "name": "aharada-education",
  "version": "1.0.0",
  "private": true,
  "description": "Aharada Education Platform",
  "main": "server.js",
  "scripts": {
    "start": "node server/server.js",
    "build": "npm --prefix server install",
    "postinstall": "npm --prefix server install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
